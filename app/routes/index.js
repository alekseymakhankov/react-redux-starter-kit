import Layout from 'components/Layout';
import FirstPage from 'components/MainPage';

const routes = [
  {
    component: Layout,
    path: '/'
  },
  {
    component: FirstPage,
    path: '/firstpage'
  }
];

export default routes;
